statsmodels>=0.14.0
pmdarima>=2.0.0
lightgbm>=4.0.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
pyyaml>=6.0.0
optuna>=3.0.0        # optional for LightGBM tuning — graceful fallback to grid search if absent
prophet>=1.1.5       # optional — wrapped in try/except; skipped if not installed
